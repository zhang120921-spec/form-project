## 2026-08-02 — Pro player restructuring & TierBadge removal

### Completed Tasks (#101–#106)
1. **is_pro flag** — Added `is_pro INTEGER DEFAULT 0` to users table schema + migration. Famous golfers in seed.ts marked with `is_pro = 1`. Backend `buildPublicProfile()` returns `isPro` field. `/api/public-profiles` supports `?filter=app|pro|all`.
2. **TierBadge removal** — Removed TierBadge component imports and usages from: OverviewPage, LeaderboardPage, PublicProfilePage, ProfilePage, PlayerCard, CompactPlayerRow, ShareModal. Also cleaned up unused `rankIndex` variables.
3. **Board tabs** — LeaderboardPage rewritten with two tabs: "App Users" (non-pro public profiles, sorted by rating) and "Compare to Pros" (user's rating + all pro players with gap display).
4. **Fair Match tabs** — FairMatchPage refactored: main component wraps a tab bar with "Fair Match" (existing calculator, pros filtered out) and "Compare to Pros" (user rating banner + pro list with rating gaps).
5. **Rankings friends-only** — RankingsPage filters `!p.isPro`. OverviewPage and ProfilePage also filter pros from rank calculations. FairMatchCalculator player grid filters pros.
6. **AI pro refresh endpoint** — Added `POST /api/ai/refresh-pros` (admin-only) that uses AI to reassess pro golfer plus-handicaps, with rule-based fallback. Stores last refresh timestamp in config table.

### Key Architecture Changes
- `PlayerState` type (web/src/lib/types.ts) now includes `isPro?: boolean`
- Backend `/api/replay` augments each player with `isPro` from DB after replay
- `usePublicProfiles(filter)` hook accepts "app" | "pro" | "all" parameter
- Database: `is_pro` column added via ALTER TABLE migration for existing DBs

### Server Status
- Backend: http://localhost:3001 (task JGb5Ju)
- Frontend: http://localhost:5173 (task KY5S2l)
- Database: 23 players (10 pro, 13 app), 9 courses, 68 rounds
- All 136 runnable tests pass (5 pre-existing import-resolution failures)

## 2026-08-02 — Seed data cleanup & Profile CSS fix (#107–#111)

### Issues Fixed
1. **Removed pros from "You"'s friends list** — Changed seed.ts friendship loop to only connect "You" with demo players + bots (not famous pros). Famous pros still form their own network among themselves.
2. **Removed fake mixed rounds** — Deleted the 4 `mixedRounds` that paired "You" with pro players (e.g., "You" 78 vs Rory McIlroy 68). Total rounds dropped from 68 to 64.
3. **Made more demo players public** — Set `isPublic: true` for Darren Lu, Wei Chen, Marcus Tan, Aaron Koh, Sam Ellis (demo) and James Lim, Kenji Sato, Tom Baker (bots). Board now shows 9 public users instead of 1.
4. **Fixed Profile rank badge CSS** — Added `position: relative` to `.section` in ProfilePage.module.css. The `.rankBadge` uses `position: absolute` but its container lacked `position: relative`, causing the badge to escape its section and create a visual overlap bug.

### Files Modified
- `server/src/db/seed.ts` — Friendship loop, mixed rounds removal, isPublic flags for demo players and bots
- `web/src/pages/ProfilePage.module.css` — Added `position: relative` to `.section`

### Server Status (updated)
- Backend: http://localhost:3001 (task dgSrPW, restarted after re-seed)
- Frontend: http://localhost:5173 (task KY5S2l, still running — HMR picks up CSS change)
- Database: 23 players, 9 courses, 64 rounds (was 68 — 4 fake mixed rounds removed)

## 2026-08-02 — Single-file build (#112)

### What was done
Built the frontend into a single self-contained HTML file (`form-golf.html`, 465 kB) using `vite-plugin-singlefile`. Changes:
1. **api.ts** — BASE URL now detects `file://` protocol and uses `http://localhost:3001/api` instead of relative `/api`
2. **App.tsx** — Uses `HashRouter` when on `file://` protocol, `BrowserRouter` otherwise
3. **server/index.ts** — CORS now allows `"null"` origin for `file://` access
4. **vite.config.ts** — Added `viteSingleFile()` plugin + `assetsInlineLimit: 100000000`
5. Output: `/Users/michaelzhang/WorkBuddy/2026-08-02-12-07-39/dusk-v3/form-golf.html`

### Usage
Open `form-golf.html` in a browser while the backend server is running on `localhost:3001`. Login: `you@demo.golf` / `golf123`.

## 2026-08-02 — Overview section spacing fix (#113)

### Issue
- `.section` CSS class in `OverviewPage.module.css` only had `border-bottom: 1px solid var(--rule)` with no vertical spacing between sections.
- Sections (Your Rival, Top Form, Who to play next, Recent Rounds) visually merged together.

### Fix
- Added `padding-bottom: var(--space-xl)` (32px) to `.section` in `web/src/pages/OverviewPage.module.css` (line 390-393).
- Each section now has clear vertical breathing room after its content.
