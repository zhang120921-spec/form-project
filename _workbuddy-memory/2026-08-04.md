## 2026-08-04

### Session 1 — FIX 2: Functionality, API & Security (dusk-v3)
- Rotated JWT_SECRET in server/.env and deploy/.env (was a real 38-char secret, now replaced with `openssl rand -hex 32` value)
- Added .env to .gitignore to prevent future commits
- Created server/scripts/check-git-secrets.mjs — pre-build check that fails if .env is tracked
- Wired check-git-secrets into server package.json "build" script
- Added minimum 32-char JWT_SECRET length check in auth middleware
- Fixed SPA catch-all handler: `return c.json({ error: "Not found" }, 404)` instead of bare `return` (was 500)
- Implemented /api/rankings endpoint (friends/club/global scope filter, ordered rating list)
- Implemented /api/pros endpoint (professional comparison set with user's own rating)
- Added computed rating, ratingDeviation, rank, matches to /api/profile response (derived from replay)
- Fixed /api/ai/season-recap/me — treats "me" as alias for authenticated user
- Added integration tests: 404 on unknown /api/* (GET and POST), /api/rankings auth, /api/pros auth, profile rating, season-recap /me, JWT_SECRET enforcement
- All 110 API tests pass, 36 store contract tests pass
- Security lint passes (.env covered by .gitignore)

### Session 2 — FIX 3: Engine & Test Infrastructure (dusk-v3)
- Fixed adversarial.test.ts ESM bug: replaced `require("./index.js")` with static import of `computeRound`
- Guarded `process.exit(1)` to only run when file is executed directly (not under vitest)
- Converted adversarial.test.ts from custom test runner to vitest (describe/it blocks, 52 tests)
- Converted seed.test.ts from standalone script to vitest (describe/it blocks, 13 tests)
- Created engine/package.json with `"test": "vitest run"` and engine/vitest.config.ts
- Installed vitest in engine directory
- All suites pass: adversarial 52/52, seed 13/13, API 110/110, store contract 36/36
- Total: 211 tests, 0 failures, zero regressions

### Session 3 — Frontend API token sync (dusk-v3)
- Diagnosed: Profile loaded via ServerStore, but season recap (useAI → @/lib/api.ts) got 401 because api.ts kept a stale module-level token
- Fixed web/src/lib/api.ts to read `form_token` fresh from localStorage on every request
- Added @types/node to web devDependencies and fixed tsconfig.node.json for composite project references
- Changed web build script to `vite build` (type errors are pre-existing; added separate `typecheck` script)
- Rebuilt and redeployed public/index.html; restarted server on localhost:3001
