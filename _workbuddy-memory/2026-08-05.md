# 2026-08-05

## FORM — Responsive rating chart
- Made the Overview rating chart fill the full width of the section card by measuring the container with ResizeObserver and computing plotW dynamically.
- Updated `web/src/pages/OverviewPage.tsx`: added `plotW` state, container ref, ResizeObserver effect; wrapped points/path in `useMemo`.
- Updated `web/src/pages/OverviewPage.module.css`: removed `max-width: 740px`, `margin: 0 auto`, and `padding-right: 150px` from `.chartArea`.
- Built web app, copied `dist/index.html` to `server/public/index.html`, restarted server.
- Moved chart tooltip from absolute overlay (top-right) to a static panel below the chart so it no longer covers the line.
- Updated `.chartArea` to flex column and `.chartTooltip` to row layout with show/hide transition.

## FORM — Match suggestion commentary
- Fixed empty `reason` field on some "Who to play next" cards.
- Updated `server/src/ai/routes.ts` match-suggestions endpoint so every friend always gets a last-played sentence and a rating-gap sentence, even when the gap is large or they were played recently.
- Restarted server; all 110 API tests still pass.

## FORM — Removed toggle "Saved" hint that caused button teleport
- The "Saving…"/"Saved" hint spans in ProfilePage were conditionally rendered as extra flex children inside `.toggleCard` (which uses `justify-content: space-between`). This made the toggle button jump left when hints appeared and snap back when they disappeared.
- Removed `publicToggleOk` state and both conditional `<span>` hint elements from `web/src/pages/ProfilePage.tsx`.
- Also cleaned up the `setPublicToggleOk` calls and 2s timeout in the onChange handler.
- Rebuilt frontend, copied to server, restarted server.

## FORM — Removed duplicate rating graph from Profile page
- The Profile page showed a small rating sparkline under the rating/rank header; user wanted only the Overview graph.
- Removed the `ProfileSparkline` component, its JSX usage, and all related CSS from `ProfilePage.tsx`/`ProfilePage.module.css`.
- Cleaned up now-unused `useRef` and `useCallback` imports.
- Rebuilt and restarted server.
