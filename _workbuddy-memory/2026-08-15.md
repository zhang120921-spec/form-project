# 2026-08-15

## FORM — Feature catalog brief
- Explored the full dusk-v3 codebase (pages, components, API, AI features, admin).
- Produced a page-by-page function brief for the user: 10 user pages + extras (AI integration, admin console, engine).
- Saved the brief to `dusk-v3/docs/FEATURES.md`.
- Rewrote FEATURES.md for a Chinese golf coach audience: full Chinese, plain golf-friendly language (no tech jargon), added project overview at top.
- Fixed FORM admin page authentication: it was not sending any auth token, so all `/api/admin/*` calls returned 401 and the dashboard showed 0 users/rounds.
- Updated `server/public/admin/index.html`: added login screen (`/api/auth/login`), JWT storage in `localStorage`, `Authorization: Bearer` header on all admin API calls, logout button, and real admin display name.

## FORM — Quick Entry parsing fix
- Root cause: `server/.env` has a placeholder AI key (`sk-rotated-replace-with-a-fresh-key-from-deepseek`), so all DeepSeek AI calls return 401 and fall through to regex parser.
- The regex fallback parser was too limited — only matched "shot 82" / "with Darren" patterns, so natural input like "Michael 78, Darren 81" produced zero results.
- Fixed `server/src/ai/service.ts`: added placeholder-key detection (skips AI call entirely, avoids 401 latency).
- Rewrote `regexParseRound()` in `server/src/ai/routes.ts`: now handles name+score pairs ("Michael 78"), stableford scores ("36pts"), date parsing (ISO, DD/MM/YYYY, "yesterday", "last Saturday"), keyword patterns, and standalone scores.
- Tested 5 input patterns — all parse correctly with high confidence.
- Note: for full AI-quality parsing, user needs to replace the placeholder key with a real DeepSeek API key in `server/.env`.

## FORM — Quick Entry friend matching + UI cleanup
- Fixed `LogRoundForm.tsx`: parsed names now match against the current user (sets `scores["self"]`) AND friends using full-name, first-name, and last-name matching.
- Previously, the current user's own score was never set from the parse result (only friends were matched), and friend matching only used substring `includes`.
- Removed the "Try:" example input buttons from the Quick Entry UI.
- Added `currentUserName` prop to `LogRoundForm` (passed from `LogRoundPage` via `user?.displayName`).

## FORM — Admin page blank fix
- Screenshot showed `localhost:3001/admin` completely blank; console error: "Account is not an administrator" during auto-login.
- Root cause: `/api/auth/login` response did not include `isAdmin`, so `admin/index.html` rejected the auto-login and returned without rendering anything.
- Fixed `server/src/index.ts`: added `isAdmin: !!user.is_admin` to login response.
- Hardened `server/public/admin/index.html`:
  - `login()` now checks `data.user?.isAdmin`.
  - Safe `JSON.parse` for stored admin user.
  - Auto-login failures fall back to the login form instead of a blank page.
  - `renderDashboard()` errors now show a visible reloadable error message.
- Restarted the backend server; verified admin page renders dashboard with 31 users, 27 rounds, 18 courses.

## FORM — Admin i18n language toggle
- Replaced dark/light theme switch in `server/public/admin/index.html` with a 中文/EN language toggle.
- Added an `I18N` dictionary (~150 keys) and `t()` function; translated navigation, labels, modals, toasts, and config into Chinese.
- Verified toggle works; screenshots captured in both languages.

## FORM — Replace user-app theme toggle with Chinese/English language switch
- Replaced the light/dark theme switch in the user-facing React app (`dusk-v3/web`) with a 中文/EN language toggle.
- Added `web/src/lib/i18n.tsx`: `t()` English-source-key translator, `useLang()` subscription hook, `toggleLang()` persisted in `localStorage` (`form_lang`).
- Replaced theme toggle in `web/src/components/Shell.tsx`; language persists and remounts page content on toggle.
- Added language toggle to `AuthPage.tsx` (renders outside Shell) with a `langBtn` style.
- Parallel agents internationalized ~30 source files (pages, components, lib helpers like `rivalries.ts`, `fair-match.ts`, `narration.ts`, `connectivity.ts`, `useAI.ts`) by wrapping user-visible strings in `t()`.
- Built a 428-entry Chinese dictionary covering navigation, stats, forms, modals, errors, match-play/stroke-play terminology, and the sandbag explainer.
- Ran `vite build` and copied `web/dist/index.html` to `server/public/index.html`.
- Verified with Playwright: English sidebar shows Overview/Rankings/Rounds/Log Round/Fair Match/Friends/Profile; after toggle, Chinese sidebar shows 总览/排名/球局/记球/公平对战/球友/我的; selection persists after reload; no console errors.

## FORM — Trial readiness: cleanup + admin resets tab + verification
- User reported "no friends in michael account" and "API not working". Diagnosed: API was healthy all along; the 80 friendships/27 rounds belonged to 20 removed Singapore demo accounts (intentional trial cleanup, `dusk-v3/server/src/db/cleanup-trial.cjs`). mike's account is now an intentional clean slate.
- Fixed `/api/admin/password-resets` 403 bug: route only had `adminMiddleware`; added `authMiddleware` first (it sets `userId` that adminMiddleware requires). Verified live via `tsx watch` auto-reload → returns `{"resets":[]}`.
- Added a "Password Resets" (密码重置) tab to `server/public/admin/index.html`: i18n keys en/zh, `loadResets()`, tab entry, panel with copy-token button (`navigator.clipboard`), post-render hook. Syntax-checked inline script with `node --check` (70,953 bytes).
- Rebuilt web bundle (PIPL consent checkbox + Chinese recap parsing live) and copied `web/dist/index.html` → `server/public/index.html`.
- Verified end-to-end: admin login works (token issued), courses API returns 18 Shanghai courses, student self-register with consent works (created + deleted `trial-check@test.golf`), forgot-password stores token in `token_plain` retrievable by admin.
- Trial state now: 13 users (admin, mike, son, 10 @famous.golf pros), 0 rounds, 0 friendships, 18 courses — ready for ~20 Shanghai academy students to self-register at localhost:3001.


