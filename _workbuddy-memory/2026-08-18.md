# 2026-08-18

## FORM — Admin panel cache fix
- User screenshot showed `localhost:3001/admin` missing the new "Password Resets" tab even though it was in `server/public/admin/index.html`.
- Root cause: browser cached the old admin HTML; the file itself already contained the tab and `curl /admin` returned the new content (verified `has_resets: true`).
- Fixed `server/src/index.ts`: added `Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate`, `Pragma: no-cache`, `Expires: 0` to the `/admin` route so the coach always gets the latest admin build.
- `tsx watch` auto-reloaded; verified response headers include the no-cache directives and `/api/admin/password-resets` still returns `{"resets":[]}` after restart.
- Next step for user: hard-refresh the browser (`Cmd+Shift+R` / `Ctrl+Shift+R`) to clear the stale cached admin page.

## FORM — AI disabled state
- Admin AI Config panel shows "Disabled" because `server/.env` still has the placeholder `AI_API_KEY=sk-rotated-replace-with-a-fresh-key-from-deepseek`.
- Quick Entry still works via the improved regex fallback parser (name+score, date, stableford, etc.).
- To enable AI parsing for the trial, the user needs to replace the placeholder with a real DeepSeek API key; left unchanged pending user decision/credentials.
